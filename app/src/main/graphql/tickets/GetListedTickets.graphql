query GetListedTickets($eventId: ID) {
  listedTickets(eventId: $eventId) {
    id
    eventId
    ownerId
    tierId
    state
    purchasePrice
    listingPrice
    currency
    transferLock
    version
    issuedAt
    expiresAt
    listedAt
    isListed
    canBeListed
  }
}
