query GetMyTickets($filters: TicketFilters) {
  myTickets(filters: $filters) {
    id
    eventId
    ownerId
    tierId
    state
    purchasePrice
    listingPrice
    currency
    transferLock
    version
    issuedAt
    expiresAt
    listedAt
    deletedAt
    isListed
    canBeListed
  }
}
