query GetTicket($id: ID!) {
  ticket(id: $id) {
    id
    eventId
    ownerId
    tierId
    state
    purchasePrice
    listingPrice
    currency
    transferLock
    version
    issuedAt
    expiresAt
    listedAt
    deletedAt
    isListed
    canBeListed
  }
}
